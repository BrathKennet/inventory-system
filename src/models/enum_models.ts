export enum TypeDeleteForm {
  CATEGORY,
  PRODUCT,
  SUPPLIER,
  LOT,
  CLIENT,
  SALE,
}

export enum TypeButton {
  PRIMARY,
  DANGER,
}

export enum TypeToast {
  SUCCESS,
  ERROR,
}

export enum Hidden {
  SM,
  MD,
  LG,
}
