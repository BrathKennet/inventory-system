export enum TypeDeleteForm {
  CATEGORY,
  PRODUCT,
  SUPPLIER,
  LOT,
  CLIENT,
  SALE,
  TRANSACTION,
}

export enum TypeButton {
  PRIMARY,
  DANGER,
}

export enum TypeToast {
  SUCCESS,
  ERROR,
}

export enum Hidden {
  SM,
  MD,
  LG,
}

export enum TypeTransaction {
  ADD,
  SOLD,
  REMOVE,
}
