export enum TypeDeleteForm {
  CATEGORY,
  PRODUCT,
  SUPPLIER,
  LOT,
}

export enum TypeButton {
  PRIMARY,
  DANGER,
}

export enum TypeToast {
  SUCCESS,
  ERROR,
}

export enum Hidden {
  SM,
  MD,
  LG,
}
